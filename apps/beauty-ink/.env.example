# ============================================================
# beauty-ink 环境变量模板
# 复制本文件为 .env 并填入真实值：cp .env.example .env
# 注意：.env 已被 .gitignore 忽略，严禁提交到版本库
# ============================================================

# DeepSeek API Key（必填，chat 问答时使用）
# 获取地址：https://platform.deepseek.com/
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# 本地 Embedding 模型（必填，ingest 与 query 必须一致）
# 常见选择：
#   BAAI/bge-m3           约 2GB，效果最佳（多语种 / 长文本）
#   BAAI/bge-small-zh-v1.5 约 100MB，轻量验证用
# 注意：更换模型后需删除 data/chroma 重新入库
EMBEDDING_MODEL=BAAI/bge-m3

# ---- 以下为可选项（留空则使用代码内默认值）----

# 重排模型（FlagEmbedding 加载，首次使用需下载约 2GB）
# RERANKER_MODEL=BAAI/bge-reranker-v2-m3

# DeepSeek OpenAI 兼容接口地址
# DEEPSEEK_BASE_URL=https://api.deepseek.com

# DeepSeek 模型名
# DEEPSEEK_MODEL=deepseek-chat
