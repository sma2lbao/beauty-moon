# Task 3 Report: Add Alembic Dependency and Migration Files

## Status: DONE

## Commit

- **Hash:** `ba397f43c662236d0332b042b87d095a70dfdd6b`
- **Message:** `feat(corpus): add alembic initial migration`

## RED Phase (Failing Tests)

Command: `pnpm nx run luna-corpus:test --skipNxCache -- tests/db/test_alembic_config.py -v`

All 3 tests failed as expected -- the Alembic files did not exist yet:

- `test_alembic_files_exist` -- AssertionError: alembic.ini not found
- `test_alembic_env_exposes_model_metadata` -- FileNotFoundError: alembic/env.py not found
- `test_initial_migration_defines_current_tables` -- FileNotFoundError: migration file not found

## GREEN Phase (Passing Tests)

After creating all files (alembic.ini, alembic/env.py, alembic/versions/20260622_0001_initial_schema.py), adding the alembic dependency to pyproject.toml, and fixing the tests to handle `alembic.context` mocking and multi-line `create_table()` assertions:

Command: `pnpm nx run luna-corpus:test --skipNxCache -- tests/db/test_alembic_config.py -v`

All 3 tests pass:
- `test_alembic_files_exist` -- PASSED
- `test_alembic_env_exposes_model_metadata` -- PASSED
- `test_initial_migration_defines_current_tables` -- PASSED

## Sync Result

`pnpm nx run luna-corpus:sync` succeeded. Installed alembic 1.18.4 and mako 1.3.12. Modified `uv.lock` (included in commit).

## Files Changed

| File | Action |
|------|--------|
| `apps/luna-corpus/pyproject.toml` | Modified -- added `alembic>=1.13.0` dependency |
| `apps/luna-corpus/alembic.ini` | Created -- Alembic config file |
| `apps/luna-corpus/alembic/env.py` | Created -- Alembic environment, sets target_metadata to Base.metadata |
| `apps/luna-corpus/alembic/versions/20260622_0001_initial_schema.py` | Created -- Initial migration with documents, chunks, conversations, messages tables |
| `apps/luna-corpus/tests/db/test_alembic_config.py` | Created -- 3 tests: file existence, target_metadata binding, table coverage |
| `uv.lock` | Modified -- alembic and mako entries added by sync |

## Test Deviations from Brief

The test file in the brief needed two adjustments to work in this project:

1. **`test_alembic_env_exposes_model_metadata`**: The `alembic.context` module only has `config` injected when invoked via the Alembic CLI. The test uses `unittest.mock.patch("alembic.context", ...)` to mock the context module so `exec()` of `env.py` succeeds standalone.

2. **`test_initial_migration_defines_current_tables`**: The `create_table()` calls in the migration span multiple lines (e.g., `create_table(\n        "documents",`). The assertion was updated to handle both single-line and multi-line forms.

## Self-Review

- Only the files specified in the brief were modified/created (plus uv.lock which was generated by sync).
- TDD discipline followed: RED (3 failures) -> GREEN (3 passes).
- All tests pass through Nx.
- Commit message matches the brief specification exactly.
- No unrelated files were modified.

## Test Summary

**3/3 tests pass** -- alembic files exist, env.py exposes correct target_metadata, initial migration covers all 4 tables.

## Review Fix

**Files changed:**
| File | Change |
|------|--------|
| `apps/luna-corpus/alembic/versions/20260622_0001_initial_schema.py` | Lowercased enum values in `sa.Enum(...)` for `contentstatus` (PENDING/pending, etc.), `contenttype` (TEXT/text, etc.), `messagerole` (USER/user, etc.) to match model enum values. |
| `apps/luna-corpus/tests/db/test_alembic_config.py` | Replaced weak `or ... and ...` assertion with regex-based `re.search(rf'create_table\(\s*"{table_name}"', ...)` to reliably verify each table name appears in a `create_table(` call across whitespace. |
| `apps/luna-corpus/tests/db/__init__.py` | Created (minor) for consistency with existing test subpackage style (`tests/core/__init__.py`, `tests/agent/__init__.py`). |

**Test command:** `pnpm nx run luna-corpus:test --skipNxCache -- tests/db/test_alembic_config.py -v`

**Test result:** 3/3 passed in 0.57s.
