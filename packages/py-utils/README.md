# py-utils

Python 工具库示例，由 `@nxlv/python:uv-project` 生成。

- 包管理：uv（`uv.lock` 已提交）
- 测试：pytest（含覆盖率 / HTML 报告）
- Lint / Format：ruff
- 构建：hatchling → `dist/`（wheel + sdist）

常用命令（在仓库根目录执行）：

```sh
npx nx test py-utils
npx nx lint py-utils
npx nx build py-utils
```
